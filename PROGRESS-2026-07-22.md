# 유니크 코드 관리 시스템 — 진행 현황 보고서

- **작성일:** 2026-07-22
- **프로젝트:** unique-code-system (유니크 코드 채번/발급/파기/결번복원 관리)
- **GitHub:** https://github.com/zeekcomputer-lang/unique-code-system (PUBLIC)
- **현재 버전:** v0.3 (MVP 4단계 완료 + 운영 피드백 3건 반영)
- **단계:** 로컬 데모 가능 · 운영 전환 전 단계

---

## 1. 문서 유효성 검증 결과

내부 문서(md)와 실제 소스코드의 정합성을 대조 검증하였음. **전 항목 일치.**

| 문서 주장 | 코드 실측 | 결과 |
|-----------|-----------|------|
| 432개 고정 풀, O/I/0 제외 | `codes.py` assert 432 통과, EXCLUDED {O,I},{0} | ✅ 일치 |
| 1순위 A1~Z9(216) / 2순위 1A~9Z(216) | primary+secondary 구성 확인 | ✅ 일치 |
| API 11개 엔드포인트 | routes.py 라우트 데코레이터 11개 확인 | ✅ 일치 |
| 화면 4종 (index/request/admin/dashboard) | frontend 4 HTML + JS 4종 존재 | ✅ 일치 |
| 기본 포트 백엔드 8099 / 프론트 8989 | 배치 주입값 일치 (코드 기본은 8000, 문서에 명시됨) | ✅ 일치(문서화됨) |
| Prefix 정규식 `^[A-Za-z0-9]{1,16}$` + 자동 대문자 | 실동작 검증 통과 | ✅ 일치 |

**결론:** README.md / HANDOFF.md v0.3 / backend README / ui-design-system.md 내용은 현재 코드 상태와 유효하게 부합함.

---

## 2. 실동작 검증 (fakeredis 로컬 구동)

이번 점검에서 서버를 실제 기동하여 핵심 워크플로우를 재검증함.

| # | 시나리오 | 결과 |
|---|----------|------|
| 1 | 서버 기동 · `/health` → queue_size 432, redis ok | ✅ |
| 2 | 대시보드 통계 (total 432 / waiting 432 / usage 0%) | ✅ |
| 3 | 의뢰 접수 → `REQ-0001` PENDING | ✅ |
| 4 | 승인+발급 prefix=APP2024 → **APP2024A1** (score 1) | ✅ |
| 5 | 파기 A1 → returned_to_queue=true | ✅ |
| 6 | **결번 복원** — peek 최상단 다시 A1(score 1) 제자리 복귀 | ✅ |
| 7 | 소문자 prefix `v2` → 자동 대문자 → **V2A1** | ✅ |
| 8 | Prefix 위반(`개발`) → HTTP 422 거부 | ✅ |
| 9 | 강제 채번 base Z9 + prefix 8K → **8KZ9** (force_issued=true) | ✅ |

**결론:** 채번 엔진의 핵심 불변식(순차 발급, 원자성, 파기 시 원순번 복원)과 Prefix 정책이 정상 동작함.

---

## 3. 기술 스택

- **백엔드:** Python 3.10+ · FastAPI · Uvicorn · Redis(ZSET) · JSON 파일 DB
- **프론트엔드:** HTML5 · Vanilla JS · Bootstrap 5.3 · FontAwesome 6.4 · Noto Sans KR
- **동시성:** Redis `ZPOPMIN`/`ZADD nx` 원자성 + JSON I/O RLock + atomic write
- **의존성 원칙:** jQuery/SPA 프레임워크 0 dependency

---

## 4. 채번 규칙 (불변)

- 영문자 24자 (A~Z 중 O, I 제외) × 숫자 9자 (1~9, 0 제외)
- 1순위 영문+숫자 216개 (A1~Z9) + 2순위 숫자+영문 216개 (1A~9Z) = **총 432개**
- 발급: `ZPOPMIN` 순차 · 파기: 원래 score로 `ZADD nx` 제자리 복원
- 최종 코드 = prefix(1~16자) + base(2자) = 최대 18자

---

## 5. 알려진 한계 / 다음 작업 후보

### 한계
- 인증/권한 없음 — Admin 화면 개방 상태
- 의뢰서 REJECT 라우트 미구현 (PENDING/APPROVED만 동작)
- pytest 정식 스위트 없음 (수동 라운드트립 검증만)
- 운영용 Docker/systemd 미작성
- 백엔드 코드 기본 포트(8000)와 배치 기본값(8099) 불일치

### 권장 후속 작업 (우선순위순)
1. 의뢰서 REJECT API (`POST /api/requests/{id}/reject`)
2. Admin/Dashboard 인증 (Basic Auth 또는 토큰 헤더)
3. pytest 스위트 (채번/파기/롤백/동시성)
4. Docker Compose (app + redis)
5. JSON DB 백업/스냅샷 정책
6. 백엔드 기본 포트 8099로 통일

---

## 6. 종합 판단

- 문서-코드 정합성 **유효**, 실동작 검증 **전 항목 통과**.
- 현 상태는 **로컬 데모/시연 즉시 가능** 수준의 안정적 MVP.
- 운영 전환을 위해서는 인증·REJECT API·자동 테스트·컨테이너화가 선결 과제.
- 작업 착수 시 진입점: `HANDOFF.md` v0.3 → §11(TODO) → §12(운영 주의사항).

---

_점검 수행: OpenClaw · 2026-07-22_
